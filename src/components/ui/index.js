/**
 * UI Components - Barrel Export
 * Los componentes duplicados son importados de @luridlf/prisma-ui-lib
 * Los componentes únicos se mantienen localmente
 */

// Componentes de la librería
export { Button, Input, Card, Badge, Alert, Spinner, Modal } from '@luridlf/prisma-ui-lib';

// Componentes únicos del proyecto
export { default as UserAvatar } from './UserAvatar';
